#include<stdio.h>
int main()
{
	int n,i,temp=0;
	printf("Enter a number:");
	scanf("%d",&n);
	
	for(i=2;i<=n;i++)
	{
		if(n%i==0)
		{
			temp++;
		}
	}
	if(temp==0 && n!=1)
	{
		printf("%d is a prime number",n);
	}
	else
	{
		printf("%d is not a prime number",n);
	}
}
